name: resume-parser
channels:
  - defaults
  - conda-forge
dependencies:
  # Python version
  - python=3.10

  # Conda version note (You can install latest conda if needed)
  # Conda itself is managed outside the environment, but we recommend >=23.3
  # To check: conda --version

  # Core libraries
  - numpy=1.26.2
  - pandas=2.1.1
  - scikit-learn=1.3.2
  - tqdm=4.66.1

  # PyTorch with CUDA
  - pytorch=2.1.0
  - torchvision=0.16.1
  - torchaudio=2.1.1
  - cudatoolkit=12.8  # matches your nvcc / CUDA version

  # Keras and preprocessing
  - keras=2.12.0

  # NLP
  - spacy=3.6.0
  - pip
  - pip:
      - en-core-web-lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.6.0/en_core_web_lg-3.6.0-py3-none-any.whl
      - pytorch-pretrained-bert==0.6.2
      - seqeval==1.2.2